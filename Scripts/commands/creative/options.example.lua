---@type CommandsMod_Commands_Options
local opt = {
    params = {
        on = {
            aliases = { "1" }
        },
        off = {
            aliases = { "0" }
        },
        on_with_achievement = {
            aliases = { "on-with-achievement", "wa", "w/a" }
        },
    }
}

return opt
