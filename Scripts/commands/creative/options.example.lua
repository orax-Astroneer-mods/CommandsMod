---@type CommandsMod_Commands_Options
local opt = {
    params = {
        on = {
            aliases = { "1" }
        },
        off = {
            aliases = { "0" }
        },
        on_with_achievements = {
            aliases = { "on-with-achievements", "wa", "w/a" }
        },
    }
}

return opt
